# Scraper ham çıktısı
urunler_raw.json

# Claude Design splash animasyon kaynakları (React JSX + editör araçları).
# Vanilla'ya çevrildi (index.html/style.css); React kaynakları repoya GİRMEZ.
_tasarim_taslak/

# Python
__pycache__/
*.pyc
*.pyo
.env

# IDE
.vscode/
.idea/

# Test dosyaları
# KURAL: test_<özellik>.py / test_<özellik>.mjs = KALICI regresyon testi, repoda durur
#        ve CI kapısına girer (bkz. deploy.yml test job).
#        Tek seferlik keşif script'i yazacaksan kesif_* önekini kullan (aşağıda ignore'lu).
# Searlo API sondaları sonda_* önekine taşındı (2026-08-21): "test" DEĞİL, canlı
# api.searlo.tech'e istek atıyorlar (kredi tükendi) — CI test_* glob'una SIZMASINLAR
# diye hem yeniden adlandırıldı hem burada ignore'lu. sonda_searlo.py içinde gömülü
# bir API anahtarı var (hiç commit edilmedi, öyle kalsın).
firsat_kod.txt
test_export.xlsx
sonda_debug.py
sonda_resim_mini.py
sonda_searlo.py
*.test.py
supabasepas.txt
*.secret
*.env
.env*

# 2026-07 — keşif / pilot / migrate (CLAUDE.md kuralı: repo'ya gitmemeli)
supabasepw.txt
kesif_*.py
kesif_a101_ham.html
a101_pilot_*.py
migrate_*.py
data/a101_*.json

# 2026-07 - Vite build pipeline
node_modules/
dist/
/public/

# 2026-08 - hub sayfalari (zam/market/kategori/hal), turetilmis cikti
.hub/

# superpowers ilerleme defteri (gecici calisma dosyasi)
.superpowers/


# Guvenlik denetim raporu — kapatilmamis KRITIK XSS (B1) somuru haritasi iceriyor.
# Repo PUBLIC; B1 duzeltilip yayina cikana kadar git'e GIRMEZ (kamuya acilmasin).
DENETIM_GUVENLIK.md

# 2026-08 - Supabase CLI yerel durumu (org id, pooler host, surum meta). Sir
# DEGIL ama repoya girmemeli. NOT: Edge Function KAYNAGI supabase/functions/**
# commit EDILIR; yalnizca .temp/ ve yerel gizli dosyalar haric tutulur.
supabase/.temp/
supabase/.env
supabase/.branches/
